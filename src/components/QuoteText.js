import React from "react";

function QuoteText ({content}) {

    return (
        <div id="quote-text">
            <span id="text">{content}</span>
        </div>
    );
}

export default QuoteText;